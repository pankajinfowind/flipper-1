name: flipper_login
description: 'A login package for flipper app'
version: 0.0.2

homepage: 'https://yegobox.com'

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0 <2.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: "^0.5.0"
  firebase_auth: "^0.18.0+1"
  flutter_blue: ^0.7.1+1
  device_info: ^0.4.0+4
  flutter_scan_bluetooth: ^1.1.0
  wifi_info_plugin: ^1.0.6
  flutter_bluetooth_serial: ^0.2.2
  sms_autofill: ^1.2.3
  # redux: ^4.0.0
  # flutter_redux: ^0.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  built_value_generator: ^7.0.9
  build_runner: ^1.10.1
# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:


  assets:
    - packages/flipper_login/assets/cell.png
    - packages/flipper_login/assets/mobile.png

# flutter packages pub run build_runner watch --delete-conflicting-outputs  --enable-experiment=non-nullable