import { PosInfiniteScrollDirective } from './pos-infinite-scroll.directive';

describe('PosInfiniteScrollDirective', () => {
  it('should create an instance', () => {
    // const page=null;
    // const directive = new PosInfiniteScrollDirective(page);
    // expect(directive).toBeTruthy();
  });
});
